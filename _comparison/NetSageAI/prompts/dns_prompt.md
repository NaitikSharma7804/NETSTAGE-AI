# DNS Troubleshooting Guidance

Focus on Layer 7 DNS concepts:
- Host DNS client configuration (`ipconfig /all`, static DNS server IP)
- Router DNS lookup (`ip domain-lookup`, `ip name-server <ip>`)
- Route reachability to DNS server destination
- DNS server service status in Packet Tracer
